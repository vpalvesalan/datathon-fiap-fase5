datathon-grupo-xx/
├── .github/
│   └── workflows/
│       └── ci.yml                      # [Etapa 2] Pipeline de CI: lint (ruff) → type check (mypy) → security (bandit) → pytest (≥60% coverage)
│
├── .env.example                        # [Segurança] Template de variáveis de ambiente (MLFLOW_URI, OPENAI_API_KEY, LANGFUSE_KEY) — nunca commitar o .env real
├── .pre-commit-config.yaml             # [Qualidade] Hooks locais: ruff + mypy + bandit rodam antes de cada commit, espelhando o CI
├── Makefile                            # [DX] Atalhos: make train | make test | make serve | make data — avaliador deve subir tudo em < 2 min
│── logs/
├── data/                               # [Etapa 1 e 3] Diretório gerenciado pelo DVC — nenhum arquivo de dados é commitado no Git
│   ├── raw/
│   │   ├── ibov/                       # [Etapa 1] CSVs brutos baixados pelo yfinance via make_dataset.py
│   │   └── agent_docs/                 # [Etapa 2] PDFs e textos-fonte usados pelo pipeline RAG
│   ├── processed/
│   │   ├── ibov/                       # [Etapa 1] Tensores/arrays com janelas LSTM prontas para treino
│   │   └── agent_db/                   # [Etapa 2] Vector Store persistido (ChromaDB ou FAISS) com embeddings dos documentos
│   ├── holdout_test/                   # [Etapa 1] Conjunto de teste imutável do LSTM — nunca tocado durante desenvolvimento
│   └── golden_set/                     # [Etapa 3] ≥ 20 pares (query, expected_answer, contexts) para avaliação RAGAS
│
├── notebooks/
│   └── 01_eda_ibov.ipynb               # [Etapa 1] EDA exploratória: distribuições, sazonalidade, correlações, insights para feature engineering
│   └── 02_lstm_training_report.ipynb
├── configs/                            # [Experiment Management] Arquivos YAML centralizam hiperparâmetros — elimina valores hardcoded no código
│   ├── model_config.yaml               # [Etapa 1] Parâmetros do LSTM e baseline: lookback_window, units, epochs, learning_rate, batch_size
│   └── monitoring_config.yaml          # [Etapa 3] Thresholds de drift: psi_warning=0.1, psi_retrain=0.2, latency_p99_ms=500
│
├── src/
│   ├── ibov_pipeline/                  # 🟢 DOMÍNIO 1 — Modelo preditivo de séries temporais (Etapa 1)
│   │   ├── __init__.py                 # [Obrigatório] Torna o diretório um pacote Python importável pelos testes e pela API
│   │   ├── config.py                   # [Etapa 1] Carrega e valida model_config.yaml com Pydantic Settings
│   │   ├── make_dataset.py             # [Etapa 1] Baixa dados do yfinance, valida schema, salva em data/raw/ibov/ — rastreado pelo DVC
│   │   ├── feature_engineering.py      # [Etapa 1] Aplica MinMaxScaler, gera janelas deslizantes, retorna tensores (X, y)
│   │   ├── train_baseline.py           # [Etapa 1] Treina baseline Scikit-Learn (ex: LinearRegression, Ridge) com MLflow tracking completo
│   │   ├── train_lstm.py               # [Etapa 1] Treina modelo LSTM/Keras com MLflow: loga params, metrics, artefato do modelo
│   │   ├── register_model.py           # [Model Registry] Registra modelo treinado no MLflow Model Registry com tags obrigatórias de governança
│   │   └── retrain.py                  # [GAP 07] Champion-challenger: carrega champion do Registry, treina challenger, promove se delta_mape ≥ 0.5%
│   │
│   ├── agent_pipeline/                 # 🔵 DOMÍNIO 2 — Agente LLM com RAG (Etapa 2)
│   │   ├── __init__.py                 # [Obrigatório] Torna o diretório um pacote Python importável
│   │   ├── react_agent.py              # [Etapa 2] Agente ReAct (LangChain/LlamaIndex) com prompt padronizado e ≥ 3 tools
│   │   ├── tools.py                    # [Etapa 2] ≥ 3 tools customizadas: ex: IBOVForecastTool (chama LSTM), RAGTool, MarketContextTool
│   │   └── rag_retriever.py            # [Etapa 2] Pipeline RAG: embedding → Vector Store → retriever → geração de resposta com contexto
│   │
│   ├── serving/                        # 🟠 SERVIÇO UNIFICADO — Expõe ambos os domínios via HTTP (Etapas 2 e 3)
│   │   ├── __init__.py                 # [Obrigatório] Torna o diretório um pacote Python importável
│   │   ├── app.py                      # [Etapa 2] FastAPI com endpoints documentados: POST /predict, POST /agent/query, GET /health
│   │   ├── schemas.py                  # [Etapa 2] Modelos Pydantic para validação de input/output de todos os endpoints
│   │   └── Dockerfile                  # [Etapa 2] Container de serving: imagem base Python, instala dependências, expõe porta 8000
│   │
│   ├── monitoring/                     # 🟡 OBSERVABILIDADE — Métricas operacionais e de qualidade (Etapa 3)
│   │   ├── __init__.py                 # [Obrigatório] Torna o diretório um pacote Python importável
│   │   ├── drift_detection.py          # [GAP 06] Evidently: calcula PSI em features do IBOV e predições — dispara alerta se PSI > 0.2
│   │   └── telemetry.py                # [GAP 01] Langfuse para métricas LLM (faithfulness, latência) + Prometheus para métricas operacionais
│   │
│   └── security/                       # 🔴 SEGURANÇA E GOVERNANÇA (Etapa 4)
│       ├── __init__.py                 # [Obrigatório] Torna o diretório um pacote Python importável
│       └── guardrails.py               # [Etapa 4] InputGuardrail (detecta prompt injection) + OutputGuardrail com Presidio (remove PII do output)
│
├── tests/                              # [Etapas 1 e 2] Quality gates — CI falha se coverage < 60%
│   ├── conftest.py                     # [GAP 04] Fixtures sintéticas compartilhadas: sample_ohlcv, sample_agent_query — nunca usar dados reais
│   ├── test_ibov_features.py           # [Etapa 1] Testa schema (pandera), ausência de nulls, shape correto das janelas LSTM
│   ├── test_agent.py                   # [Etapa 2] Testa se cada tool retorna o tipo esperado e se o agente completa sem exceção
│   └── test_api.py                     # [Etapa 2] Testa endpoints com FastAPI TestClient: status codes, schema de resposta, casos de erro
│
├── evaluation/                         # [Etapa 3] Avaliação offline da qualidade do sistema RAG + LLM
│   ├── ragas_eval.py                   # [Etapa 3] Calcula 4 métricas RAGAS: faithfulness, answer_relevancy, context_precision, context_recall
│   ├── llm_judge.py                    # [Etapa 3] LLM-as-a-judge com ≥ 3 critérios: correção factual, relevância de negócio, ausência de alucinação
│   └── ab_test_prompts.py              # [Etapa 3] Compara ≥ 3 variações de system prompt no golden set e reporta delta nas métricas RAGAS
│
├── docs/                               # [Etapa 4] Documentação de governança — todos os arquivos são critério de aceite explícito
│   ├── MODEL_CARD_IBOV.md              # [Etapa 4] Documenta o LSTM: dados de treino, métricas, limitações, uso pretendido
│   ├── SYSTEM_CARD_AGENT.md            # [Etapa 4] Documenta o agente: arquitetura, tools, riscos, guardrails implementados
│   ├── OWASP_MAPPING.md                # [Etapa 4] ≥ 5 ameaças do OWASP Top 10 LLM mapeadas com mitigações implementadas
│   ├── RED_TEAM_REPORT.md              # [Etapa 4] ≥ 5 cenários adversariais testados: prompt injection, jailbreak, data exfiltration
│   └── LGPD_PLAN.md                    # [Etapa 4] Plano de conformidade LGPD: mapeamento de dados, base legal, retenção, anonimização
│
├── dvc.yaml                            # [Etapa 1] Define o DAG de dados: make_dataset → feature_engineering → train — reprodutível com dvc repro
├── docker-compose.yml                  # [Etapa 3] Orquestra FastAPI (8000) + Prometheus (9090) + Grafana (3000) com um único docker compose up
└── pyproject.toml                      # [Qualidade] Dependências gerenciadas (Poetry/uv) + config de ruff, mypy, bandit, pytest e coverage