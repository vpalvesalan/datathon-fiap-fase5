# --- Dados ---
# Dados brutos e tensores de treino — grandes, não necessários em serving
data/raw/
raw_data/
data/holdout_test/
data/processed/ibov/X_train.npy
data/processed/ibov/X_test.npy
data/processed/ibov/y_train.npy
data/processed/ibov/y_test.npy
# Artefatos de serving (model_lstm.keras, scaler.joblib, agent_db/, drift_report.json)
# são versionados no git via `cache: false` no dvc.yaml — necessários para o build do Render.
*.csv
*.xlsx
*.parquet
*.feather
*.zip
*.tar.gz

# --- Modelos Treinados ---
# Apenas formatos que nunca precisam ir ao Render; .keras e .joblib são permitidos
# quando dentro de data/processed/ (versionados explicitamente no git).
*.pkl
*.h5
*.model
*.pt
/mlruns
/logs

# --- Notebooks e Ambiente ---
# Checkpoints e outputs do Jupyter Notebook
.ipynb_checkpoints/
*/.ipynb_checkpoints/*

# Cache Python
__pycache__/
*.py[cod]

# --- Ambiente Virtual e Credenciais ---
# Ambientes virtuais e chaves de API/Senhas
.env
venv/
.venv/
env/
.settings/

# --- IDEs/Sistemas Operacionais ---
.DS_Store
.vscode/
.idea/
.claude

# --- Outros ---
temp.txt